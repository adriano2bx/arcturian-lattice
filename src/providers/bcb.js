export class BcbProvider {constructor({fetchFn=globalThis.fetch}={}){this.id='bcb_sgs';this.fetchFn=fetchFn;}async series({code,dateFrom,dateTo}){const u=new URL(`https://api.bcb.gov.br/dados/serie/bcdata.sgs.${encodeURIComponent(code)}/dados`);u.searchParams.set('formato','json');if(dateFrom)u.searchParams.set('dataInicial',toBr(dateFrom));if(dateTo)u.searchParams.set('dataFinal',toBr(dateTo));let r;try{r=await this.fetchFn(u.toString(),{headers:{accept:'application/json'}})}catch(e){return{ok:false,provider:this.id,reason:'network_error',detail:String(e?.message??e)}}if(!r.ok)return{ok:false,provider:this.id,reason:'upstream_error',status:r.status};return{ok:true,provider:this.id,series:await r.json()};}}
function toBr(v){const m=String(v).match(/^(\d{4})-(\d{2})-(\d{2})$/);return m?`${m[3]}/${m[2]}/${m[1]}`:String(v)}
